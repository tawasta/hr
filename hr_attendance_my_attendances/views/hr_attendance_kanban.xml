<?xml version="1.0" encoding="utf-8"?>

<odoo>
    <record id="view_hr_attendance_kanban" model="ir.ui.view">
        <field name="name">hr.attendance.kanban.no.create</field>
        <field name="model">hr.attendance</field>
        <field name="groups_id" eval="[(4, ref('hr_attendance_my_attendances.my_attendances'))]"/>
        <field name="inherit_id" ref="hr_attendance.view_hr_attendance_kanban"/>
        <field name="arch" type="xml">
            <kanban position="attributes">
                <attribute name="edit">false</attribute>
                <attribute name="create">false</attribute>
            </kanban>
        </field>
    </record>
</odoo>
